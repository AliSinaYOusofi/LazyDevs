const router = require("express").Router();


// get account credentials
router.get("/my_details", async (req, res) => {
    const {user_id} = req.body;

    try {

    } catch(e) {
        console.log(e);
    }
})